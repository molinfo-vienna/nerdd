apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: broker-config-serialize-jobs
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: broker-store
  target:
    name: broker-config-serialize-jobs
  dataFrom:
    - extract:
        key: broker-config
