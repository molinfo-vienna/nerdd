apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: broker-config-backend
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: broker-store
  target:
    name: broker-config-backend
  dataFrom:
    - extract:
        key: broker-config
