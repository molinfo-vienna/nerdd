apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

components:
  - ../../../_common/nerdd-module/envs/dev10

configMapGenerator:
  - name: app-config
    behavior: merge
    literals:
      - image=ghcr.io/molinfo-vienna/cypstrate:latest
